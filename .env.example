# The simulator runs entirely on in-repo synthetic data; no secrets are required.
# Override the modeled tax year at build time if additional years are added to src/lib/tax-year.
NEXT_PUBLIC_DEFAULT_TAX_YEAR=2025
